// Función principal
function main() {
    // Obtengo el body, que es el padre del componente
    const bodyEl = document.body;

    // Función para montar el componente contact
    addContactComponent(bodyEl);
}

// EJECUCION
main();
